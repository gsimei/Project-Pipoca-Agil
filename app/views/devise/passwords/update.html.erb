<% if resource.errors.empty? %>
  <h1>Senha atualizada com sucesso!</h1>
  <p>Sua senha foi alterada com sucesso. Agora você pode fazer login com sua nova senha.</p>
  <a href="<%= new_user_session_path %>">Clique aqui para fazer login</a>
<% else %>
  <h1>Erro ao atualizar a senha</h1>
  <ul>
    <% resource.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
